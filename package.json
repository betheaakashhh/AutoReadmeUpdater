{
  "name": "readme-sync-bot",
  "version": "1.0.0",
  "description": "GitHub App that keeps README.md in sync using code analysis — no AI API required",
  "main": "src/index.js",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "probot run ./src/index.js",
    "dev":   "probot run ./src/index.js"
  },
  "dependencies": {
    "probot": "^13.0.0",
    "dotenv":  "^16.4.5"
  }
}
